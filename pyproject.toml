[project]
name = "lite-llm-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # PR #38279 fix for bedrock openai.gpt-5.6-luna thinking -> reasoning.effort mapping
    # 1.100.0rc1 is earliest RC with fix; pin to RC until 1.100 stable released, then bump to >=1.100
    "litellm[proxy]==1.100.0rc1",
]
